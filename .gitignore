# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
venv/
.venv/

# IDE
.vscode/
.idea/
.claude/

# OS
.DS_Store
Thumbs.db
*.fuse_hidden*

# Database — tracked in git for Streamlit Cloud deployment
# nascar.db

# Personal contest ledger — private financial data, never committed.
# (nascar.db is public + overwritten daily by the refresh job, so contest
# entries live in their own local DB.)
contests.db

# Secrets (never commit API keys)
.streamlit/secrets.toml

# Local-only files
exports/
cache/
*.log

# Internal reference doc — keep local, do not publish to the repo
APP_REFERENCE.md
