# Pinned to the versions tested locally (2026-06-10). Bump deliberately —
# Streamlit Cloud reinstalls on every redeploy, so loose pins can pull
# breaking versions at arbitrary times.
streamlit==1.56.0
pandas==3.0.2
numpy==2.4.4
plotly==6.6.0
requests==2.33.1
beautifulsoup4==4.14.3
lxml==6.0.2
